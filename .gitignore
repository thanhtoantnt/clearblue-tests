# regeneratable by scripts/run_bench.sh; can be 0.1–5 GB each
/persist/

# local benchmark outputs — never commit/upload
/results/


# editor/OS noise
.DS_Store
*.swp

# rebuilt bitcode stays local (rebuild via scripts/build_old_bc.sh / build_pr_bc.sh)
*.bc
